on:
  push:
    branches:
      - main

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - name: React Native Build
        uses: bamachoub/react-native-build-action@v1.0.0
        with:
          platform: android
          
